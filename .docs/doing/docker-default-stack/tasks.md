# Tasks

- [x] 1. Update the default Docker build and startup targets to include both bot and scheduler services.
- [x] 2. Document the new default Docker behavior and persistence model in the README.
- [x] 3. Verify the Make targets render the expected Compose commands and sync docs to the implemented behavior.
