# Tasks: source-identity-cards

- [x] 1. Add explicit source-item linkage in runtime/storage.
- [x] 2. Add cached latest-item preview metadata support in the web API.
- [x] 3. Expose a source preview endpoint that joins source identity, latest item, and health state.
- [x] 4. Replace the Sources explanatory cards with image-first preview cards.
- [x] 5. Preserve safe config-only handling for `x_inbox`.
- [x] 6. Polish the preview card UI hierarchy, action model, and fallback states.
- [x] 7. Run verification (`make test`, `npm --prefix web run test`, `npm --prefix web run build`).
