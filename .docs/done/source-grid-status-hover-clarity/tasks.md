# Tasks: Source Grid Status Hover Clarity

- [x] Reduce desktop table columns to `Type`, `Source`, `Status`, `Actions`.
- [x] Move diagnostics (`last_error`, `last_seen`, `hint`) into status hover/focus detail.
- [x] Keep row-level `Edit` and `Delete` actions visible.
- [x] Update UI tests for the new table contract.
- [ ] Run frontend verification (`npm --prefix web run test`, `npm --prefix web run build`).
- [ ] Move docs from `.docs/doing` to `.docs/done` after verification.
